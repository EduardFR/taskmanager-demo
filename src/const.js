const COLORS = ['black', 'yellow', 'blue', 'green', 'pink'];

const FilterType = {
  ALL: 'all',
  OVERDUE: 'overdue',
  TODAY: 'today',
  FAVORITES: 'favorites',
  REPEATING: 'repeating',
  ARCHIVE: 'archive',
};

const UpdateType = {
  INIT: 'INIT',
};

const SortType = {
  DEFAULT: 'default',
  DATE_DOWN: 'date-down',
  DATE_UP: 'date-up',
};

export {COLORS, FilterType, UpdateType, SortType};
